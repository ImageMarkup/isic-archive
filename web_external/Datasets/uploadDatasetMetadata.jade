.isic-form-page.isic-upload-dataset-metadata-container
  h2.isic-page-title Upload dataset metadata

  .isic-form-container
    .isic-form-group
      h3 Select dataset
      p.
        Select the dataset for which to upload metadata:
      .form-group#isic-upload-dataset-metadata-dataset-select-container
    .isic-form-group
      h3 Upload metadata
      .isic-upload-description-container
        | Upload a CSV file of image metadata. The <i>filename</i> column
        | should contain an image filename (including file extension). All
        | other columns are considered to be clinical metadata; these other
        | column names may not contain the period (.) character.
      .isic-upload-container
        .isic-upload-widget-container
        .isic-upload-reset-container
           .isic-upload-list
           button#isic-upload-reset.btn.btn-secondary.btn-md Reset
    .isic-form-group
      h3 Validate metadata
      p.
        This section displays any problems with the uploaded metadata.
        Press the button below to validate the metadata.
        Once the metadata validates successfully, you will be given
        an option to save the metadata to the dataset.
      .isic-upload-dataset-metadata-validate-button-container.text-center
        button#isic-upload-dataset-metadata-validate.btn.btn-primary.btn-md Validate
      .isic-upload-dataset-metadata-validation-container
        #isic-upload-dataset-metadata-validation-container
