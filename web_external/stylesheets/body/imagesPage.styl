/* page-wide styles */
#galleryContent {
  position: relative;
  min-height: 12em;
  height: calc(100vh - 13em);
  display: flex;
  width: 100%;
  flex-wrap: nowrap;
  -ms-user-select: none;
  -moz-user-select: none;
  -webkit-user-select: none;
}
#galleryContent .flexRow {
  display: flex;
  width: 100%;
  flex-wrap: nowrap;
}
#galleryContent .flexColumn {
  display: flex;
  flex-direction: column;
  height: 100%;
  flex-wrap: nowrap;
}
#galleryContent .fixedWidth {
  min-width: 32em;
  max-width: 32em;
  flex-basis: 32em;
  overflow-y: auto;
}
#galleryContent .spacer,
#galleryContent .growing {
  flex-grow: 1;
}

#galleryContent .pane {
  position: relative;
  padding: 0.5em;
  margin: 0.5em;
  border: 1px solid #dddddd;
  border-radius: 3px;
}

/* Study pane */
#galleryContent #studyPane {

}

/* Study text pane */
#galleryContent #studyTextPane {

}

/* Image wall */
#galleryContent #imageWall {
  overflow-y: auto;
  overflow-x: hidden;
  min-width: 32em;
}
#galleryContent #imageWall image {
  cursor: pointer;
}
#galleryContent #imageWall rect#highlightOutline {
  fill: none;
  stroke: #00ABFF;
  stroke-width: 4px;
}

/* Paging pane */
#galleryContent #pagingPane {
  flex-basis: 3em;
  height: 3em;
  max-height: 3em;
  min-height: 3em;
  display: flex;
  flex-wrap: nowrap;
  align-items: center;
}
#galleryContent #pagingPane img {
  width: 1.75em;
  height: 1.75em;
  margin: 1em;
  border: 1px solid transparent;
  border-radius: 3px;
  padding: 3px;
  cursor: pointer;
}
#galleryContent #pagingPane img.selected {
  filter: url(#recolorImageTo00ABFF);
}
#galleryContent #pagingPane img:hover {
  filter: url(#recolorImageTo444499);
  border: 1px solid #e2e2e2;
  background: linear-gradient(to bottom, #fff 60%, #f6f6f6 100%);
}
#galleryContent #pagingPane #pageSizeControl {
  margin: 1em;
  white-space: nowrap;
}
#galleryContent #pagingPane #pageSizeControl input {
  text-align: center;
  width: 6em;
}

/* Histogram pane */
#galleryContent #histogramPane {

}

/* Image details pane */
#galleryContent #imageDetailsPane {

}
