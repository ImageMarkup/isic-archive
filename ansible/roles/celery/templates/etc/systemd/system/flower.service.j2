[Unit]
Description=Flower Service

[Service]
User={{ ansible_user_id }}
Group={{ ansible_user_id }}
ExecStart={{ girder_virtualenv }}/bin/flower --port=5555 --loglevel=info -A isic_archive.tasks
Restart=on-failure
Type=simple

[Install]
WantedBy=multi-user.target
