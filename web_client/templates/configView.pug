.g-config-breadcrumb-container
form#isic-config-form
  .form-group
    label.control-label(for="isic-config-demo-mode") Demo Mode
    p Newly registered users will automatically have full access to all phases.
    input#isic-config-demo-mode.input-sm.form-control(type="checkbox")
  .g-settings-form-container
    h4 ZIP Upload to S3
    p.
      Amazon Web Services (AWS) resource configuration to support clients
      uploading ZIP files of images directly to S3.
    .form-group
      label.control-label(for="isic-config-zip-upload-user-access-key-id") User access key ID
      input#isic-config-zip-upload-user-access-key-id.input-sm.form-control(
        type="text", placeholder="Enter AWS user access key ID")
    .form-group
      label.control-label(for="isic-config-zip-upload-user-secret-access-key") User secret access key
      input#isic-config-zip-upload-user-secret-access-key.input-sm.form-control(
        type="text", placeholder="Enter AWS user secret access key")
    .form-group
      label.control-label(for="isic-config-zip-upload-s3-bucket-name") S3 bucket name
      input#isic-config-zip-upload-s3-bucket-name.input-sm.form-control(
        type="text", placeholder="Enter AWS S3 bucket name")
    .form-group
      label.control-label(for="isic-config-zip-upload-role-arn") Upload role ARN
      input#isic-config-zip-upload-role-arn.input-sm.form-control(
        type="text", placeholder="Enter AWS IAM role ARN")
    .form-group
      label.control-label(for="isic-config-zip-upload-assume-role-duration-seconds") Assume role duration (seconds)
      input#isic-config-zip-upload-assume-role-duration-seconds.input-sm.form-control(
        type="text", placeholder="Enter number of seconds")
  p#isic-config-error-message.g-validation-failed-message
  input.btn.btn-sm.btn-primary(type="submit", value="Save")
